{
  "name": "json-server-db",
  "version": "1.0.0",
  "description": "JSON Server API deployed on Vercel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "vercel-build": "echo 'No build required'"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
